/**
 * A number derived from a substring of π. A <code>PiSlice</code> consists of a
 * lower and upper bound from which a substring of π can be derived as a
 * <code>long</code> or <code>int</code>.
 */

package lol.pislice;
